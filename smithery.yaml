startCommand:
  type: sse
  configSchema:
    type: object
    default: {}
    description: No config needed
  exampleConfig: {}
  commandFunction: |-
    (config) => ({ command: 'logi-mcp-server', args: ['sse'] })
